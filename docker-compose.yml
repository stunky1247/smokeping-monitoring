---
services:
  smokeping:
    image: linuxserver/smokeping:latest
    container_name: smokeping
    hostname: smokeping
    dns:
      - 8.8.8.8
      - 1.1.1.1
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/AKDT
    volumes:
      - ./smokeping/config:/config
      - ./smokeping/data:/data
    ports:
      - 80:80
    restart: always
